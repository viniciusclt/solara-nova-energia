# Configuração de Ambiente - Solara Nova Energia
# Copie este arquivo para .env e configure as variáveis necessárias

# =============================================================================
# SUPABASE - OBRIGATÓRIO
# =============================================================================
# URL do projeto Supabase
VITE_SUPABASE_URL=https://your-project.supabase.co

# Chave pública (anon key) do Supabase
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# =============================================================================
# APLICAÇÃO
# =============================================================================
# Versão da aplicação
VITE_APP_VERSION=1.0.0

# Ambiente (development, staging, production)
VITE_APP_ENVIRONMENT=development

# =============================================================================
# FEATURES - OPCIONAL (padrão: habilitado)
# =============================================================================
# Habilitar OCR (Tesseract.js)
VITE_ENABLE_OCR=true

# Habilitar integração financeira
VITE_ENABLE_FINANCIAL=true

# Habilitar integração Google Sheets
VITE_ENABLE_GOOGLE_SHEETS=true

# Habilitar logs de auditoria
VITE_ENABLE_AUDIT_LOGS=true

# =============================================================================
# SEGURANÇA
# =============================================================================
# Dias de retenção de logs
VITE_LOG_RETENTION_DAYS=30

# Habilitar logging seguro (automático em produção)
VITE_ENABLE_SECURE_LOGGING=true

# Mascarar dados sensíveis (automático em produção)
VITE_MASK_SENSITIVE_DATA=true

# =============================================================================
# INTEGRAÇÕES EXTERNAS - OPCIONAL
# =============================================================================
# Google API Key (para Google Sheets)
# VITE_GOOGLE_API_KEY=your-google-api-key

# Chave de API para OCR externo (se usar serviço externo)
# VITE_OCR_API_KEY=your-ocr-api-key

# =============================================================================
# DESENVOLVIMENTO - APENAS PARA DEV
# =============================================================================
# Habilitar modo debug
VITE_DEBUG_MODE=true

# Habilitar mock de dados
VITE_ENABLE_MOCK_DATA=false

# URL do Supabase local (se usar desenvolvimento local)
# VITE_SUPABASE_URL=http://localhost:54321
# VITE_SUPABASE_ANON_KEY=your-local-anon-key

# =============================================================================
# NOTAS IMPORTANTES
# =============================================================================
# 1. NUNCA commite o arquivo .env no repositório
# 2. Use apenas VITE_ como prefixo para variáveis do frontend
# 3. Chaves sensíveis devem ser configuradas no ambiente de deploy
# 4. Para produção, configure as variáveis no Vercel/Netlify/etc
# 5. Teste sempre a configuração antes do deploy